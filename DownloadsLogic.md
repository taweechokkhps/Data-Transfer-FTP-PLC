# รายละเอียด Logic การดาวน์โหลดและการตรวจสอบไฟล์ซ้ำ (Download & Duplicate Check Pipeline)

เอกสารนี้อธิบายลำดับขั้นตอนการทำงาน (Logic Pipeline) ในการเชื่อมต่อ, คัดกรอง, ตรวจสอบไฟล์ซ้ำ, ดาวน์โหลด และแปลงไฟล์ของโปรแกรม **FTP Data Transfer**:

---

## 1. การเชื่อมต่อ PLC (Connection & Protocol Fallback)

* **ดึงการตั้งค่า**: IP, Port, Username, Password และโหมด FTP (`Auto`, `Active (PORT)`, `Passive (PASV)`)
* **การจัดการโหมด Active / Passive**:
  * PLC ในโรงงานอุตสาหกรรม (เช่น Keyence) ส่วนใหญ่จะรองรับเฉพาะโหมด **Active (PORT)**
  * เมื่อตั้งค่าเป็น `Auto` หรือ `Active` ระบบจะสั่ง `set_pasv(False)` เพื่อใช้โหมด Active
  * หากเปิดโหมด Passive แล้ว PLC ตอบกลับด้วย Error **`502 PASV command not implemented`** ระบบมีกลไก **Auto-Fallback** สลับมาเป็นโหมด Active (PORT) ให้ทันทีโดยอัตโนมัติ

---

## 2. การคัดกรองไฟล์ตามวันที่ (Date Filtering & Filename Parsing)

* วนลูปสำรวจโฟลเดอร์ของแต่ละ Machine/Station ใน PLC (เช่น `/0_CARD/log0/` ของ `MC1`, `MC2`)
* ดึงรายชื่อไฟล์ทั้งหมดบน PLC ด้วยคำสั่ง `nlst()` และกรองเฉพาะนามสกุลที่เลือก (เช่น `.txt`, `.csv`)
* **การถอดรหัสวันที่จากชื่อไฟล์**:
  * ตรวจสอบชื่อไฟล์ด้วย Regular Expression ในรูปแบบ **`DDMMYY`** ที่นำหน้าชื่อไฟล์:
    * ตัวอย่าง: `020425.txt` ➔ วันที่ `02/04/2025`
* **โหมดการกรอง**:
  * **โหมด Date Range**: คัดเลือกเฉพาะไฟล์ที่มีวันที่อยู่ในช่วงที่ระบุ (เช่น `01/09/2026` ถึง `07/09/2026`)
  * **โหมด All Files**: นำไฟล์ทั้งหมดที่มีในตู้มาประมวลผล

---

## 3. โครงสร้างโฟลเดอร์ปลายทาง (Folder Hierarchy)

ระบบจะจัดเก็บไฟล์อย่างเป็นหมวดหมู่โดยอัตโนมัติ:

```text
{Target Directory}/
└── LINE 1/
    ├── MC1/
    │   └── (01-09-2026 - 07-09-2026)/
    │       ├── plaintext/    <-- เก็บไฟล์ต้นฉบับ .txt ที่ดาวน์โหลดมาจาก PLC
    │       └── csv/          <-- เก็บไฟล์ที่ผ่านการแปลงเป็น .csv มาตรฐาน UTF-8
    └── MC2/
        └── (01-09-2026 - 07-09-2026)/
            ├── plaintext/
            └── csv/
```
*(กรณีโหมด All Files โฟลเดอร์จะตั้งชื่อตามช่วงวันที่จริงของไฟล์ เช่น `(02-04-2025 - 07-09-2026) ALL` หรือ `(ALL_FILES)`)*

---

## 4. โลจิคการตรวจสอบไฟล์ซ้ำความเร็วสูง (High-Performance Smart Skip Logic)

เนื่องจาก PLC มีหน่วยประมวลผลขนาดเล็กและตอบสนอง FTP ช้า ระบบจึงใช้เทคนิค **Local-First + Past-File Fast-Skip** เพื่อลดภาระของ PLC และทำให้การตรวจสอบรวดเร็วขึ้นกว่าเดิม 100 เท่า:

### โค้ดการทำงานจริงในระบบ (`core/ftp_service.py`):
```python
pure_filename = Path(filename).name
local_filepath = plaintext_dir / pure_filename
csv_filename = f"{Path(pure_filename).stem}.csv"
csv_filepath = csv_dir / csv_filename

# 1. Local-First: ตรวจสอบในเครื่องคอมพิวเตอร์ก่อนเสมอ (ความเร็ว 0.001 ms)
if local_filepath.exists() and csv_filepath.exists():
    f_date = parse_date_from_filename(pure_filename)
    today = datetime.date.today()

    # กรณี A: ถ้าเป็นไฟล์ในอดีต (เมื่อวาน หรือวันก่อนๆ) เครื่องจักรจะไม่เขียนทับอีกแล้ว
    # -> ข้ามทันที (SKIP) ใน 0 ms ไม่ต้องส่งสัญญาณไปกวน PLC เลยแม้แต่คำสั่งเดียว!
    if f_date and f_date < today:
        current_index += 1
        if progress_callback:
            progress_callback(current_index, total_files)
        continue

    # กรณี B: ถ้าเป็นไฟล์ของวันนี้ (Today) ที่อาจยังมี Log บันทึกต่อ ให้ส่งถามขนาดกับ PLC
    try:
        remote_size = self.ftp.size(filename)
        if remote_size and local_filepath.stat().st_size == remote_size:
            current_index += 1
            if progress_callback:
                progress_callback(current_index, total_files)
            continue  # ข้ามถ้าขนาดเท่าเดิม ไม่ได้มี Log ใหม่เพิ่ม
    except Exception:
        pass  # หากถามขนาดไม่สำเร็จ จะเข้าสู่การดาวน์โหลดเพื่อความปลอดภัย
```

### การวิเคราะห์ลำดับการทำงาน:
1. **Local-First Check (เช็คในคอมพิวเตอร์ก่อน):**
   * ถ้าในคอมพิวเตอร์ **ยังไม่มี** ไฟล์นี้ (เพิ่งติดตั้งหรือมีไฟล์ใหม่เข้ามา) ➔ **สั่งดาวน์โหลดทันที** ไม่ต้องเสียเวลายิงถามขนาดไฟล์จาก PLC ให้หน่วง
2. **Past-File Fast-Skip (กฎของไฟล์ในอดีต):**
   * หากชื่อไฟล์เป็นวันที่ก่อนหน้าวันนี้ (เช่น `f_date < today`) และในคอมพิวเตอร์มีทั้งไฟล์ `.txt` และ `.csv` ครบแล้ว ➔ **ข้ามทันที 0 ms ไม่ส่งคำสั่ง FTP ใดๆ ไปยัง PLC เลย**
   * หากมี 500 ไฟล์ในตู้ PLC และเป็นไฟล์เก่า 499 ไฟล์ ➔ ทั้ง 499 ไฟล์จะถูกตรวจสอบเสร็จสิ้นภายในเสี้ยววินาทีเดียว!
3. **Today-File Live Check (เช็คเฉพาะไฟล์ของวันนี้):**
   * เฉพาะไฟล์ของวันที่ปัจจุบัน (`f_date == today`) เท่านั้น ที่ระบบจะส่งคำสั่ง `ftp.size()` ไปถาม PLC เพื่อดูว่าขนาดไฟล์เพิ่มขึ้นจาก Log ที่กำลังเดินเครื่องอยู่หรือไม่

---

### ตารางผลลัพธ์การตัดสินใจ (Decision Matrix):

| สถานะในคอมพิวเตอร์ | ช่วงเวลาของไฟล์ | การสื่อสารกับ PLC | ผลลัพธ์ | ความเร็ว |
|---|---|---|---|---|
| **มีครบทั้ง txt และ csv** | **วันที่ในอดีต (`< today`)** | ❌ **ไม่ส่งคำสั่งใดๆ ไปยัง PLC** | ⏩ **SKIP ทันที** | **0.001 ms (เร็วสูงสุด)** |
| **มีครบทั้ง txt และ csv** | **วันที่ของวันนี้ (`== today`)** | ยิงถามขนาดไฟล์ 1 ครั้ง (`ftp.size`) | ⏩ **SKIP** ถ้าขนาดเท่าเดิม<br>⬇️ **UPDATE** ถ้าขนาดเพิ่มขึ้น | ~50 ms (แค่ไฟล์เดียว) |
| **ยังไม่มีในคอมพิวเตอร์** | วันใดก็ได้ | ❌ **ไม่ถามขนาดล่วงหน้า** | ⬇️ **DOWNLOAD ทันที** | เริ่มโหลดทันที ไม่เสียเวลารอ |
| **มี txt แต่ไม่มี csv** | วันใดก็ได้ | ❌ **ไม่ถามขนาดล่วงหน้า** | ⬇️ **DOWNLOAD + CONVERT** | ซ่อมแซมไฟล์ให้อัตโนมัติ |

---

### ข้อดีเมื่อเทียบกับระบบทั่วไป:
1. **เร็วขึ้นกว่าเดิม 100 เท่า**: ลดคำสั่ง FTP Network Round-trip จากเดิมหลายร้อยครั้ง เหลือเพียง 1 ครั้ง (เฉพาะไฟล์วันนี้)
2. **ถนอม PLC และ Network**: ไม่ดึงทรัพยากร CPU ของ PLC ช่วยให้ PLC ทำงานควบคุมเครื่องจักรได้อย่างมีเสถียรภาพสูงสุด
3. **ไม่พลาดข้อมูลใหม่ (Live Updating)**: ไฟล์ของวันนี้ยังคงได้รับการตรวจสอบขนาดแบบ Real-time เสมอ

2. **รองรับไฟล์ Log ที่กำลังบันทึก (Live Updating Logs)**: ถ้าเครื่องจักรยังคงเขียนข้อมูลต่อลงในไฟล์ของวันปัจจุบัน ขนาดไฟล์บน PLC จะโตขึ้น ทำให้ขนาดไม่เท่ากัน ระบบจะดึงข้อมูลใหม่ล่าสุดมาอัปเดตให้โดยอัตโนมัติ
3. **ปลอดภัย 100% (Read-Only Copy)**: คำสั่งที่ใช้คือ `RETR` (อ่านข้อมูลไบต์) **ไม่มีการส่งคำสั่งลบ (`DELE`) หรือแก้ไขใด ๆ บน PLC ทั้งสิ้น** ข้อมูลบน PLC จะคงอยู่ครบถ้วนสมบูรณ์เสมอ

---

## 5. การดาวน์โหลดและบันทึกเวลา (Download & Timing)

* ดาวน์โหลดไฟล์ไบนารีจาก PLC เขียนลงในโฟลเดอร์ `plaintext/`
* จับเวลาความเร็วในการดาวน์โหลดแต่ละไฟล์เป็น **ระดับมิลลิวินาที (ms)**
* ส่ง Log แจ้งเตือนไปยังคอนโซลแบบละเอียด:
  ```text
  [09:25:34.120] [LINE 1][MC1] Downloaded 020425.txt (42.5 KB) in 152.3 ms
  ```
* อัปเดตแถบ Progress Bar และนาฬิกาจับเวลารวม `⏱ MM:SS` บนหน้าจอแบบ Real-time

---

## 6. การแปลงไฟล์เป็น CSV อัตโนมัติ (Auto CSV Conversion)

* **Auto-Detect Encoding**: รองรับรหัสภาษาไทยและรหัสมาตรฐานสากล (`UTF-8`, `CP874`, `TIS-620`, `Latin-1`)
* **Auto-Detect Delimiter**: ตรวจจับตัวคั่นข้อมูลอัตโนมัติ ไม่ว่าจะเป็น Tab (`\t`), Comma (`,`) หรือ Semicolon (`;`)
* **บันทึกเป็น UTF-8 with BOM (`utf-8-sig`)**:
  * สามารถใช้คำสั่ง `cat` / `type` ดูผ่าน Terminal ได้ทันที
  * ดับเบิ้ลคลิกเปิดผ่าน **Microsoft Excel** ได้ทันทีโดยสระและพยัญชนะภาษาไทยไม่เพี้ยน
* บันทึก Log การแปลงผล:
  ```text
  [09:25:34.210] [LINE 1][MC1] Converted to csv/020425.csv (1,250 rows) in 23.4 ms
  ```

---

## 7. จบกระบวนการ (Completion)

* เมื่อครบทุกไฟล์ จะส่งคำสั่งตัดการเชื่อมต่อ `QUIT` ปิด Session อย่างถูกต้อง
* หน้าจอ Overview จะเปลี่ยนสถานะเป็น **`● Finished`** พร้อมระบุเวลารวมที่ใช้ไป และเริ่มนับถอยหลังรอบ Auto Pull ถัดไป